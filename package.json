{
  "name": "@fiber-l402/root",
  "private": true,
  "version": "0.1.0",
  "description": "Simplified L402 blog using native fnn x402",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "typescript": "^5.6.3"
  }
}